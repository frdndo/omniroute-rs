# Dev mode: arahkan API calls ke proxy sidecar (bukan vite :5173).
# Hanya berlaku utk `vite dev` (mode=development) — build release pakai
# VITE_PROXY_BASE yang diset di beforeBuildCommand (tauri.conf.json).
VITE_PROXY_BASE=http://127.0.0.1:20129
